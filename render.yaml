services:
  - type: web
    name: eza-security
    runtime: docker
    plan: free
    autoDeploy: true

    envVars:
      - key: APP_ENV
        value: production

      - key: APP_DEBUG
        value: "false"

      - key: APP_KEY
        generateValue: true